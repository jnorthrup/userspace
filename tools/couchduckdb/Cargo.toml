[package]
name = "couchduckdb"
version = "0.1.0"
edition = "2021"

[dependencies]
duckdb = { version = "0.9", default-features = false, optional = true }
anyhow = "1.0"

[features]
default = []
duck = ["duckdb"]
