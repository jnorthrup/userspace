# LSMR Example CLI

This example demonstrates a tiny usage of the `LsmrDatabase`.

Build and run:

```bash
cargo run --example lsmr_cli -- ./example_data
```

The example will start the DB, write a sample document, then read it back.
